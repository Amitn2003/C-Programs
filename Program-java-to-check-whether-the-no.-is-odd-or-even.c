#include <iostream>
using namespace std;
int main()
{
    //Write a program  Java to check whether the no. is odd or even.
    cout<<"Enter the number: ";
    int a;
    cin>>a;
    if (a%2==0) {
        cout<<"Even";
    }
    else {
        cout<<"Odd";
    }
    return 0;
}
